export interface activeStudiesProps {
  tag: string[],
  title: string,
  meta: {
    members: string,
    nextMeeting: string,
  },
} 


export interface applyStatusProps {
  tag: string[],
  title: string,
  date: string
}